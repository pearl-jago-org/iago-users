spring:
  datasource:
    url: jdbc:postgresql://postgres.iago-dev.svc.cluster.local:5432/iago
    username: iago
    password: secret
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true


